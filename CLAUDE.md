# 秦彻专属回忆网页 - 项目说明

## 项目概述
恋与深空角色"秦彻"的专属回忆网页，黑红炫酷风，含照片墙、语录轮播、音乐播放器。

## 核心规则

### 文件结构
- `index.html` — 唯一的页面文件（HTML + CSS + JS 全在一个文件里）
- `photos/` — 照片文件夹，**不要动里面的照片**
- `music/` — 音乐文件夹，**不要动里面的音乐文件**
- `启动.bat` — 启动脚本，**不要删**

### 设计约束
- 配色：黑底 + 红色（主色 #c41e3a），**永远不要改主题色**
- 风格：暗黑炫酷，西方龙 + 乌鸦元素要保留
- 音乐播放器：底部固定，必须保持紧凑（高度 48px），**不要越改越大**

### 修改注意事项
- 添加照片：照片放进 `photos/`，然后在 `const photos = [` 数组里加文件名
- 添加音乐：MP3 放进 `music/`，然后在 `const musicFiles = [` 数组里加文件名
- 修改 CSS 时注意手机端适配（@media max-width: 768px 部分）
- 火焰粒子特效不要去掉，是页面标志性元素

### 浏览器兼容
- 本地 HTML 文件，通过 `file://` 协议打开
- 自动播放可能被浏览器拦截，已有 fallback 机制（点击页面开始播放）
- 不需要任何服务器、不需要安装任何东西